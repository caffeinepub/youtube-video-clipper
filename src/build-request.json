{
  "kind": "build_request",
  "title": "Fix admin panel to properly display user identity section and system statistics",
  "projectName": "Beast Clipping",
  "priority": "high",
  "requirements": [
    {
      "id": "REQ-93",
      "text": "Fix the AdminPanel component in frontend/src/components/AdminPanel.tsx to ensure the user identity section containing the principal ID displays correctly above the system statistics, replacing the current 'Debug Info' card that shows only principal and isOwner values",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-user-latest"
        ],
        "quotes": [
          "This is what it is showing me and no it is not showing me a user Id"
        ]
      },
      "acceptanceCriteria": [
        "AdminPanel component renders a clearly labeled 'Your Identity' or 'User ID' section at the top of the dashboard",
        "The section displays the authenticated user's full principal ID retrieved from the useInternetIdentity hook",
        "The principal ID is displayed in a readable format with proper text styling and layout",
        "A copy-to-clipboard button is present next to the principal ID",
        "The user identity section is visually distinct from the debug information and system statistics"
      ]
    },
    {
      "id": "REQ-94",
      "text": "Fix the AdminPanel component in frontend/src/components/AdminPanel.tsx to ensure system statistics (total clips count and trending clips table) render below the user identity section instead of showing only debug information",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-user-latest"
        ],
        "quotes": [
          "This is what it is showing me and no it is not showing me a user Id"
        ]
      },
      "acceptanceCriteria": [
        "System statistics section displays total clips count fetched from useAdminStats hook",
        "Trending clips table renders with columns for title, viral score, and view count",
        "Statistics section only renders after user identity section",
        "Loading states are handled gracefully with skeleton loaders or loading messages",
        "Error states display appropriate error messages without crashing the component"
      ]
    },
    {
      "id": "REQ-95",
      "text": "Remove or relocate the 'Debug Info' card in AdminPanel component that currently displays raw principal and isOwner boolean values, as this technical debug information should not be the primary content of the admin dashboard",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-user-latest"
        ]
      },
      "acceptanceCriteria": [
        "Debug Info card displaying 'Principal:', 'isOwner value:', 'isOwner type:', and 'Check browser console' is removed from the main admin panel view",
        "Debug information is either moved to browser console logging only or placed in a collapsible debug section",
        "User-facing admin panel displays only production-ready UI elements (user identity, statistics, trending clips)"
      ]
    },
    {
      "id": "REQ-96",
      "text": "Verify the useAdminStats hook in frontend/src/hooks/useAdminStats.ts is correctly fetching data from the backend and returning valid statistics without errors that would prevent the admin panel from rendering properly",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-user-latest"
        ]
      },
      "acceptanceCriteria": [
        "useAdminStats hook successfully calls backend methods getTotalClipsCount and getTrendingClipsAnalytics",
        "Hook returns loading state that resolves to data or error state",
        "React Query configuration prevents infinite loading or hanging states",
        "Error handling logs issues to console and returns error state to component"
      ]
    }
  ],
  "constraints": [
    "Do not modify frontend/src/hooks/useInternetIdentity.ts or frontend/src/hooks/useActor.ts",
    "Maintain existing authentication and authorization logic",
    "Keep all Motoko backend logic in the single main actor file"
  ],
  "nonGoals": [
    "Changing the backend admin authentication logic or principal comparison",
    "Modifying the useIsOwner hook or admin route protection",
    "Adding new admin statistics or analytics features",
    "Changing the visual theme or color scheme of the admin panel"
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}